---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: healthgatedrollouts.rollout.healthrollout.io
spec:
  group: rollout.healthrollout.io
  names:
    kind: HealthGatedRollout
    listKind: HealthGatedRolloutList
    plural: healthgatedrollouts
    singular: healthgatedrollout
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HealthGatedRollout is the Schema for the healthgatedrollouts
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HealthGatedRollout
            properties:
              additionalChecks:
                description: additionalChecks defines optional Prometheus signals
                  such as simulated GPU health.
                items:
                  description: MetricCheck defines an optional upper-bound health
                    signal from Prometheus.
                  properties:
                    maxValue:
                      anyOf:
                      - type: integer
                      - type: string
                      description: maxValue is the largest value considered healthy.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    name:
                      description: name identifies the signal in rollout status and
                        Events.
                      minLength: 1
                      type: string
                    query:
                      description: query must return one scalar or instant-vector
                        value.
                      minLength: 1
                      type: string
                  required:
                  - maxValue
                  - name
                  - query
                  type: object
                type: array
              canaryDeployment:
                description: canaryDeployment is the candidate Deployment managed
                  by this rollout.
                minLength: 1
                type: string
              cooldownSeconds:
                default: 300
                description: cooldownSeconds controls how long a rolled-back rollout
                  waits before retrying.
                format: int32
                minimum: 0
                type: integer
              errorRateQuery:
                description: errorRateQuery must return the canary HTTP error fraction
                  from zero to one.
                minLength: 1
                type: string
              evaluationIntervalSeconds:
                default: 15
                description: evaluationIntervalSeconds controls how often Prometheus
                  is queried.
                format: int32
                minimum: 5
                type: integer
              failureThreshold:
                description: failureThreshold is the number of consecutive unhealthy
                  evaluations required for rollback.
                format: int32
                minimum: 1
                type: integer
              latencyQuery:
                description: latencyQuery must return canary latency in seconds.
                minLength: 1
                type: string
              maxErrorRate:
                anyOf:
                - type: integer
                - type: string
                description: maxErrorRate is the largest healthy error fraction.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              maxLatencySeconds:
                anyOf:
                - type: integer
                - type: string
                description: maxLatencySeconds is the largest healthy latency value.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              minimumRequestCount:
                description: minimumRequestCount prevents health decisions based on
                  too little traffic.
                format: int64
                minimum: 1
                type: integer
              prometheusURL:
                description: prometheusURL is the base URL of the Prometheus HTTP
                  API.
                pattern: ^https?://
                type: string
              requestCountQuery:
                description: requestCountQuery must return the number of canary requests
                  in the observation window.
                minLength: 1
                type: string
              rollbackStableReplicas:
                default: 1
                description: rollbackStableReplicas is restored when the canary is
                  unhealthy.
                format: int32
                minimum: 1
                type: integer
              stableDeployment:
                description: stableDeployment is the trusted Deployment managed by
                  this rollout.
                minLength: 1
                type: string
              steps:
                description: steps defines the stable-to-canary replica ratios used
                  during promotion.
                items:
                  description: RolloutStep defines one approximate traffic split through
                    replica counts.
                  properties:
                    canaryReplicas:
                      format: int32
                      minimum: 1
                      type: integer
                    stableReplicas:
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - canaryReplicas
                  - stableReplicas
                  type: object
                minItems: 1
                type: array
            required:
            - canaryDeployment
            - errorRateQuery
            - failureThreshold
            - latencyQuery
            - maxErrorRate
            - maxLatencySeconds
            - minimumRequestCount
            - prometheusURL
            - requestCountQuery
            - stableDeployment
            - steps
            type: object
          status:
            description: status defines the observed state of HealthGatedRollout
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the HealthGatedRollout resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consecutiveFailures:
                description: consecutiveFailures is reset after each healthy evaluation.
                format: int32
                type: integer
              currentStep:
                description: currentStep is the zero-based index in spec.steps.
                format: int32
                type: integer
              lastEvaluationTime:
                description: lastEvaluationTime records the most recent Prometheus
                  decision.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the spec generation used for the
                  latest decision.
                format: int64
                type: integer
              phase:
                description: phase summarizes the rollout state.
                type: string
              rollbackTime:
                description: rollbackTime starts the cooldown period after a rollback.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
